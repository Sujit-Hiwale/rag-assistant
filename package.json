{
  "name": "rag-assistant",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "concurrently -n BACKEND,FRONTEND -c cyan,magenta \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "build": "npm run build --prefix frontend && echo 'Backend is TS, nothing to build here.'",
    "postinstall": "npm install --prefix frontend && npm install --prefix backend"
  },
  "dependencies": {
    "hnswlib-node": "^3.0.0"
  },
  "devDependencies": {
    "@electron/remote": "^2.1.3",
    "concurrently": "^9.2.1",
    "electron": "^39.2.6",
    "electron-builder": "^26.0.12",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.3",
    "wait-port": "^1.1.0"
  }
}
